# Production Environment Variables
NODE_ENV=production
PORT=3000

# Base de données (sera remplacé par Secrets Manager)
DB_HOST=${DB_HOST}
DB_PORT=3306
DB_NAME=maturity_assessment
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

# JWT Secret (sera remplacé par Secrets Manager)
JWT_SECRET=${JWT_SECRET}

# CORS
FRONTEND_URL=https://dev-maturity.e-dsin.fr

# Logs
LOG_LEVEL=info